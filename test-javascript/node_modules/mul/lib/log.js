import chalk from 'chalk';
export function err(message, ...optionalParams) {
  if (message instanceof Error) message = message.message;
  console.error(chalk.bold.red('error:'), message, ...optionalParams);
}
export function panic(message, ...optionalParams) {
  err(message, ...optionalParams);
  process.exit();
}
export function warn(message, ...optionalParams) {
  if (message instanceof Error) message = message.message;
  console.warn(chalk.bold.yellow('warning:'), message, ...optionalParams);
}
export default {
  err,
  panic,
  warn
};